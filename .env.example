# Supabase Configuration
# Get these values from your Supabase project settings > API

# Your Supabase project URL
NEXT_PUBLIC_SUPABASE_URL=https://your-project-ref.supabase.co

# Your Supabase project anon/public API key
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here

# Optional: Your Supabase service role key (for server-side operations)
# WARNING: Keep this secret! Only use on server-side
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# Database Configuration (Optional)
# Direct database connection URL for advanced use cases
# Usually not needed as the client handles this automatically
# DATABASE_URL=postgresql://postgres:[YOUR-PASSWORD]@db.[YOUR-PROJECT-REF].supabase.co:5432/postgres

# Development vs Production Environment
NODE_ENV=development

# For Vercel Production Deployment:
# 1. Add these same variables to your Vercel environment variables
# 2. Use the same NEXT_PUBLIC_* variables (they will be available in browser)
# 3. SUPABASE_SERVICE_ROLE_KEY should only be set for server-side operations
# 4. Set NODE_ENV=production in Vercel

# JWT Secret (Optional - for advanced auth configurations)
# SUPABASE_JWT_SECRET=your-jwt-secret-here

# Sabre API OAuth 2.0 Client Credentials
# Get these from your Sabre Developer account at https://developer.sabre.com
# Required for hotel image retrieval using GetHotelMedia API
# Note: You may need to contact your Sabre Account Representative to activate image services
SABRE_CLIENT_ID=your_sabre_client_id_here
SABRE_CLIENT_SECRET=your_sabre_client_secret_here

# Example of real credentials (DO NOT USE IN PRODUCTION):
# SABRE_CLIENT_ID=V1:abcd1234:DETN:EXT:AA
# SABRE_CLIENT_SECRET=AbCdEf12345


# Sabre API OAuth 2.0 Client Credentials
# Get these from your Sabre Developer account at https://developer.sabre.com
SABRE_CLIENT_ID=your_sabre_client_id_here
SABRE_CLIENT_SECRET=your_sabre_client_secret_here

# Sabre API Base & Auth (required for token issuance)
# For production, Sabre recommends the platform host below.
SABRE_BASE_URL=https://api.platform.sabre.com
SABRE_AUTH_PATH=/v2/auth/token

# Sabre REST API version (used when building endpoints like /v1.0.0/...)
SABRE_API_VERSION=v1.0.0

# Google Sheets API (Paragon ID 업데이트 대상 조회 등 시트 전체 행 조회용)
# 1) 서비스 계정: src/config/google-service-account.json 파일 사용 (시트에 id-285@n8n-ai-462714.iam.gserviceaccount.com 공유 필요)
# 2) 권장: 환경변수로 서비스 계정 키 주입 (배포 환경에서 파일 없이 동작)
# GOOGLE_SERVICE_ACCOUNT_BASE64=base64-of-service-account-json
# 또는
# GOOGLE_SERVICE_ACCOUNT_JSON={"type":"service_account",...}
# 3) 또는 API 키: GOOGLE_SHEETS_API_KEY 또는 GOOGLE_API_KEY 설정
# GOOGLE_SERVICE_ACCOUNT_PATH=src/config/google-service-account.json  # 기본값(로컬 파일 방식)
# GOOGLE_SHEETS_API_KEY=your-google-api-key

# Debug flags (local development only)
# SABRE_DEBUG=true prints masked logs
# SABRE_DEBUG_PLAINTEXT=true prints raw credentials (DO NOT USE in production)
SABRE_DEBUG=false
SABRE_DEBUG_PLAINTEXT=false